import React from "react";

const Separator = () => {
    return (
        <hr style={{
            backgroundColor: "darkgreen",
            margin: 10,
            borderWidth: 1,
            borderColor: "transparent",
            opacity: 0.3
        }}/>
    );
};

export default Separator